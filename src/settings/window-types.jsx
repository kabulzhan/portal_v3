// нужны для передачи через пропс окон в разные части проекта 
export const department_window = 'department-window';
export const employee_window = "employee-window";
export  const position_window = 'position-window';
export  const project_window = "project_window"

